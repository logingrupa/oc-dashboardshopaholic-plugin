1.0.0:
    - Shop dashboard - orders data source (buckets, turnover, price-type profit), new orders widget, order stat fact table
    - create_order_stats_table.php
    - add_order_positions_order_id_index.php
1.1.0:
    - Seed the Shopaholic Dashboard default layout on the main backend dashboard
    - seed_shopaholic_dashboard.php
1.2.0:
    - Single Profit metric with cost price type in settings, VAT flag on price types
    - add_price_types_vat_flag.php
1.3.0:
    - Backend translated to en, lv, ru, nb - metrics, dimensions, settings, permissions, widgets, seeded dashboard titles
1.3.1:
    - QA toolchain - pint, phpstan level 10 + larastan, phpmd, rector, pest runner, Makefile, composer qa script
1.3.2:
    - Backfill survives 512M on campaign-era stores - processor released per order, deterministic GC per chunk, memory in progress output
1.4.0:
    - Period cards replace all-time status counters - orders in period, average and median order value, cancel rate
    - update_dashboard_period_cards.php
1.5.0:
    - Store-manager stats - weekday, hour-of-day and shipping-method dimensions, canceled order value, items per order, units sold, returning-customer share, hours to ship
    - add_order_stats_manager_columns.php
1.5.1:
    - Seed the manager card row - units sold, items per order, returning share, hours to ship, canceled value on the untouched stock dashboard
    - update_dashboard_manager_cards.php
1.5.2:
    - Hover help on every dashboard card - live current and previous-period values plus a practical explanation, translated, styled as a hover card
1.5.3:
    - Seeded dashboard owned by the first superuser - CLI-seeded row was invisible in the Dashboards manage list, impossible to reorder
    - fix_seeded_dashboard_owner.php
1.5.4:
    - Owner resolver survives v1-imported stores - is_superuser holds 0/1/2 there, the boolean bind skipped the 2s; activated superusers now preferred
1.5.5:
    - Legacy report widget AJAX fix - buttons inside static dashboard widgets post no _dash_definition, so the Dash widget never bound and handlers failed with "widget has not been bound"; a backend.ajax.beforeRunHandler listener recovers the dashboard from the widget alias and binds it
